# === 🔒 Secrets ===
.env

# === 🧹 Optional helper scripts not ready for prod ===
split_data.py

# === 📊 JSON files to ignore ===
data/last_timestamps.json

# === ✅ Allow test JSON example ===
!data/last_timestamps_TEST.json

# === 📁 Ignore ALL CSV & JSON in data (except exceptions) ===
data/**/*.csv
data/**/*.json

# === ✅ Always allow .gitkeep to keep folder structure ===
!data/**/.gitkeep

# === 🧪 Ignore test_data but keep .gitkeep ===
test_data/**
!test_data/**/.gitkeep

# === ✅ Ignore backup CSVs but keep folder
backup/*.csv
backup/sql/
!backup/.gitkeep

# === ✅ Ignore all SQL backups in backup/sql but keep folder
backup/sql/
!backup/sql/.gitkeep

data/backup/sql/*
!data/backup/sql/.gitkeep

scripts/archive/

# === 🚫 Ignore ALL .sql and .gz database backups ===
*.sql
*.sql.gz


# === 🚫 Ignore Postgres dump folders ===
pg_dump/
pg_restore/

# === 🚫 Ignore archive scripts folder ===
scripts/archive/